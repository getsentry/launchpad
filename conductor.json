{
    "scripts": {
        "setup": "direnv allow && eval \"$(direnv export zsh)\" && devenv sync",
        "run": "eval \"$(direnv export zsh)\" && make serve"
    },
    "runScriptMode": "nonconcurrent"
}
